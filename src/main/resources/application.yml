spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: 
    username: 
    password: 

  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect

    show-sql: true

server:
  port: 8080

logging:
  level:
    root: debug
